# Chave de API do Google AI Studio (Gemini). Nunca commitar a chave real.
GEMINI_API_KEY=

# Modelo Gemini usado pelo backend (opcional — assume gemini-3.5-flash-lite se vazio).
GEMINI_MODEL=gemini-3.5-flash-lite
